(()=>{const v=document.getElementById('heroVideo');const b64=window.CASH_LANA_VIDEO_B64;if(!v||!b64)return;try{const raw=atob(b64);const bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);const url=URL.createObjectURL(new Blob([bytes],{type:'video/mp4'}));v.src=url;v.load();v.addEventListener('loadeddata',()=>v.play().catch(()=>{}),{once:true});window.addEventListener('pagehide',()=>URL.revokeObjectURL(url),{once:true});}catch(e){console.warn('Hero video could not be reconstructed',e);}})();
